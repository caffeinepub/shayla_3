{
  "frontend/src/utils/contentGenerator.ts": "Comprehensive URL-aware product data extraction pipeline with multi-fallback CSS selectors for 11 supported sites (Digikala, Basalam, Torob, Shylaa, Sheypoor, Snapp Shop, Amazon, eBay, AliExpress, Alibaba, Emalls), sequential CORS proxy chain with per-proxy error logging, generic content-scoring fallback, and structured GeneratedContent output including images, specs, sale price (purchasePrice × 1.4), and tags.",
  "frontend/src/utils/platformFormatters.ts": "Platform-specific content formatters for website, Instagram, Ita, and blog — all using actual extracted product name, computed sale price (purchasePrice × 1.4), verbatim specs, and image URLs from GeneratedContent.",
  "frontend/src/pages/ContentGeneratorPage.tsx": "Content generator page with a \"Back to Home\" button (using TanStack Router's useNavigate) in the header area, and an improved labeled \"پاک کردن\" (Clear) button with a Trash2 icon that resets all state (URL, content, errors, progress) and auto-focuses the URL input for rapid multi-link workflows.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; useAddHistoryEntry always creates a new unique entry (never overwrites) with timestamp-based ID, keeping the 50 most recent entries in localStorage.",
  "frontend/src/types/history.ts": "Frontend-only ContentHistoryEntry interface with string id (timestamp+random), numeric timestamp, and typed content object for localStorage-based history management.",
  "frontend/src/components/ContentHistory.tsx": "Displays the content generation history list with delete functionality, using the updated `ContentHistoryEntry` type (string `id`, number `timestamp`) and the renamed `useDeleteHistoryEntry` hook from `useQueries`.",
  "frontend/src/components/ProductUploadHelper.tsx": "New admin-facing panel that displays all 8 product fields (name, purchase price, sale price, short description, full description, specs, tags, image URLs) in a structured layout with per-field copy buttons and a \"Copy All Fields\" button that uses Clipboard API with textarea fallback.",
  "frontend/src/components/ContentOutput.tsx": "Platform-tabbed content output component with per-section copy buttons that show \"کپی شد!\" for 2 seconds and fall back to textarea execCommand when Clipboard API is unavailable.",
  "frontend/src/hooks/useSettings.ts": "Zustand store and React Query hooks for settings management — fixes the applySettings function to also update the --gold CSS variable (accent color), moves the setSettings side-effect out of queryFn into a useEffect, and ensures DOM changes are applied atomically on both load and save.",
  "frontend/src/App.tsx": "Main app router and layout — fixes AppLayout to subscribe to the Zustand settings store via a selector so it re-applies DOM changes (dark mode, font size, accent color) reactively whenever settings change, instead of only once on mount.",
  "frontend/src/pages/SettingsPage.tsx": "Settings form page — renames local state to formSettings to clarify it's form-local state, adds a loading state while backend data is fetched, adds a \"Saved!\" confirmation state on the Save button that shows for 2 seconds after a successful save, and ensures the form always initializes from the latest backend-fetched values."
}